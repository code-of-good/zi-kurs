# Zero-Knowledge Proof: История Алисы и Боба

## Сценарий

**Алиса** знает гамильтонов цикл в графе и хочет доказать это **Бобу**, не раскрывая сам цикл.

**Боб** хочет убедиться, что Алиса действительно знает цикл, но не хочет узнавать, какой именно.

**Важно:** Оба участника знают граф G (это общий входной граф). Разница в том, что:

- **Алиса** знает также гамильтонов цикл в графе G
- **Боб** знает только граф G, но не знает цикл

## Протокол (один раунд)

### Раунд 1: Коммит

**Алиса:**

1. Берет граф G и известный ей гамильтонов цикл
2. Генерирует случайную перестановку π вершин
3. Переставляет граф: создает G' = π(G) (изоморфный граф с переименованными вершинами)
4. Создает криптографический коммит к графу G':
   - Генерирует случайный salt
   - Вычисляет hash = SHA256(G' + salt)
   - Отправляет Бобу только `{hash, salt}`

**Боб:**

- Получает коммит `{hash, salt}`
- Не может узнать, какой граф G' закоммичен (hash необратим)
- Генерирует случайный challenge: **0** или **1**

### Зачем передавать hash и salt?

**Hash (хеш):**

- Это "отпечаток" графа G' - криптографическое представление данных
- SHA-256 необратим: из hash нельзя восстановить исходный граф G'
- Hash фиксирует граф G' до того, как Боб выберет challenge
- Позволяет Бобу проверить, что Алиса не изменила граф после получения challenge

**Salt (соль):**

- Случайная строка, добавляемая к данным перед хешированием
- Предотвращает атаки перебора (rainbow table attacks)
- Гарантирует, что даже для одинакового графа G' hash будет разным в разных раундах
- Без salt злоумышленник мог бы предвычислить хеши для популярных графов

**Как это работает вместе:**

1. Алиса сериализует граф G' в строку (например, отсортированные рёбра: `"0,1|0,3|1,2|..."`)
2. Добавляет случайный salt: `"0,1|0,3|1,2|..." + "a3f5b2c8..."`
3. Вычисляет hash: `SHA256("0,1|0,3|1,2|..." + "a3f5b2c8...") = "7f3a8b2c..."`
4. Отправляет Бобу только `{hash: "7f3a8b2c...", salt: "a3f5b2c8..."}`
5. Позже, когда Алиса раскрывает G', Боб может проверить:
   - Сериализует G' так же: `"0,1|0,3|1,2|..."`
   - Добавляет salt: `"0,1|0,3|1,2|..." + "a3f5b2c8..."`
   - Вычисляет hash и сравнивает с полученным
   - Если совпадает → Алиса не изменила граф после коммита ✓

### Раунд 2: Challenge

**Боб → Алиса:** "Challenge = 0" (или 1, случайно)

### Раунд 3: Response

**Алиса** отвечает в зависимости от challenge:

#### Если Challenge = 0: "Покажи перестановку"

**Алиса → Боб:**

- Перестановку π: `[2, 0, 1, 3]` (как переименованы вершины)
- Все рёбра графа G': `[(0,1), (1,2), (2,3), (3,0)]`

**Боб проверяет:**

1. Перестановка валидна? (все вершины от 0 до n-1, нет дубликатов) ✓
2. Коммит соответствует G'?
   - Сериализует G' так же, как Алиса
   - Вычисляет hash(G' + salt)
   - Сравнивает с полученным hash ✓
3. G' = π(G)? (проверяет изоморфизм) ✓

**Важно:** Боб **НЕ получает** оригинальный граф G от Алисы! Боб **уже знает** граф G с самого начала протокола (это общий входной граф для обоих участников).

**Как Боб проверяет изоморфизм G' = π(G):**

- Боб использует свой **известный** граф G (который он знает с начала)
- Боб применяет перестановку π к графу G: для каждого ребра (u, v) в G проверяет наличие ребра (π(u), π(v)) в G'
- Боб также проверяет обратное: для каждого ребра (u', v') в G' проверяет наличие ребра (π⁻¹(u'), π⁻¹(v')) в G
- Если обе проверки проходят → G' действительно является переставленной версией G ✓

**Результат:** Боб убеждается, что G' - это действительно переставленный граф G, но **не узнает цикл** (Алиса не показала рёбра цикла).

#### Если Challenge = 1: "Покажи цикл"

**Алиса → Боб:**

- Все рёбра графа G': `[(0,1), (1,2), (2,3), (3,0), ...]` (чтобы Боб мог восстановить G')
- Рёбра переставленного цикла в G': `[(0,1), (1,2), (2,3), (3,0)]`
- **НЕ показывает** перестановку π

**Боб проверяет:**

1. **Проверка коммита:**

   - Восстанавливает граф G' из `permutedGraphEdges`
   - Сериализует G' так же, как Алиса (отсортированные рёбра)
   - Вычисляет `SHA256(serialized(G') + salt)`
   - Сравнивает с полученным `hash` из коммита
   - Если совпадает → Алиса не изменила граф после коммита ✓

2. **Валидность рёбер цикла:**

   - Проверяет количество: `cycleEdges.length === n` (n рёбер для n вершин) ✓
   - Проверяет диапазон: все вершины в `[0..n-1]` ✓
   - Проверяет отсутствие self-loops: `u !== v` для всех рёбер ✓

3. **Существование рёбер в графе G':**

   - Для каждого ребра `(u, v)` из `cycleEdges` проверяет: существует ли это ребро в G'?
   - Если хотя бы одно ребро цикла отсутствует в G' → доказательство отвергнуто
   - Это критично: Алиса не может показать цикл, которого нет в G' ✓

4. **Проверка гамильтонова цикла:**
   - Восстанавливает последовательность вершин из рёбер (например, `[0, 1, 2, 3]`)
   - Проверяет, что каждая вершина встречается ровно один раз
   - Проверяет, что рёбра образуют цикл (последняя вершина соединена с первой)
   - Проверяет, что все рёбра цикла существуют в G'
   - Если все проверки пройдены → это валидный гамильтонов цикл в G' ✓

**Каким образом Боб убеждается, что в G' есть гамильтонов цикл?**

Боб убеждается через **многоуровневую проверку**:

1. **Коммит гарантирует честность:** Боб проверяет, что граф G', в котором Алиса показывает цикл, это тот же граф G', который был закоммичен до выбора challenge. Это предотвращает подмену графа.

2. **Проверка существования рёбер:** Боб проверяет, что каждое ребро цикла действительно существует в графе G'. Если Алиса попытается показать несуществующее ребро, проверка провалится.

3. **Проверка структуры цикла:** Боб восстанавливает последовательность вершин из рёбер и проверяет:

   - Все n вершин присутствуют (гамильтоновость)
   - Каждая вершина встречается ровно один раз
   - Рёбра образуют замкнутый цикл (последняя вершина соединена с первой)

4. **Проверка через функцию `isValidHamiltonianCycle()`:** Боб использует алгоритм, который проверяет все свойства гамильтонова цикла одновременно.

**Результат:** Боб убеждается, что в G' есть гамильтонов цикл, но **не знает**, как он соотносится с исходным графом G (не знает π). Это ключевой момент Zero-Knowledge: Боб узнаёт о существовании цикла, но не узнаёт, какой именно цикл знает Алиса в исходном графе G.

## Множественные раунды

Чтобы снизить вероятность обмана, протокол повторяется **k раз** (например, k=5).

**Вероятность обмана без знания цикла:**

- Один раунд: 1/2 (50%)
- 5 раундов: 1/2^5 = 1/32 (3.125%)
- 10 раундов: 1/2^10 = 1/1024 (0.098%)

### Пример: 3 раунда

```
Раунд 1:
  Алиса → Боб: коммит₁ (hash₁, salt₁) для G'₁
  Боб → Алиса: challenge = 0
  Алиса → Боб: π₁, G'₁
  Боб проверяет:
    - Восстанавливает G'₁ из response
    - Проверяет коммит₁ против G'₁ (используя hash₁ и salt₁)
    - Проверяет изоморфизм G'₁ = π₁(G)
    ✓

Раунд 2:
  Алиса → Боб: коммит₂ (hash₂, salt₂) для G'₂ (НОВАЯ перестановка π₂!)
  Боб → Алиса: challenge = 1
  Алиса → Боб: рёбра цикла в G'₂ (π₂ НЕ раскрыта)
  Боб проверяет:
    - Восстанавливает G'₂ из response
    - Проверяет коммит₂ против G'₂ (используя hash₂ и salt₂)
    - Проверяет валидность цикла в G'₂
    ✓

Раунд 3:
  Алиса → Боб: коммит₃ (hash₃, salt₃) для G'₃ (ЕЩЁ ОДНА перестановка π₃!)
  Боб → Алиса: challenge = 0
  Алиса → Боб: π₃, G'₃
  Боб проверяет:
    - Восстанавливает G'₃ из response
    - Проверяет коммит₃ против G'₃ (используя hash₃ и salt₃)
    - Проверяет изоморфизм G'₃ = π₃(G)
    ✓

Результат: Все раунды прошли → Боб принимает доказательство
```

### Как работает проверка коммита в каждом раунде?

**Важно:** Каждый раунд полностью независим и имеет свой собственный коммит!

**Структура одного раунда:**

```
ProofRound = {
  commitment: { hash: "...", salt: "..." },  // Коммит ЭТОГО раунда
  response: { ... }                            // Ответ ЭТОГО раунда
}
```

**Как Боб проверяет коммит:**

1. **В каждом раунде** Алиса создаёт новую перестановку π и новый граф G'
2. Алиса создаёт коммит **именно к этому G'** этого раунда:
   ```
   Раунд i:
   - Генерирует πᵢ и G'ᵢ
   - Создаёт коммитᵢ = {hashᵢ, saltᵢ} для G'ᵢ
   - Отправляет коммитᵢ Бобу
   ```
3. Когда Алиса раскрывает G' в response, Боб проверяет коммит **этого же раунда**:

   ```
   Боб получает:
   - commitmentᵢ = {hashᵢ, saltᵢ} (из раунда i)
   - responseᵢ с G'ᵢ (из раунда i)

   Боб проверяет:
   - Восстанавливает G'ᵢ из responseᵢ
   - Сериализует G'ᵢ
   - Вычисляет SHA256(serialized(G'ᵢ) + saltᵢ)
   - Сравнивает с hashᵢ из commitmentᵢ
   - Если совпадает → коммит корректен для ЭТОГО раунда ✓
   ```

### Что значит "Восстанавливает G'ᵢ из responseᵢ"?

**Response содержит только рёбра графа, а не сам объект Graph!**

**Пример:**

```
Алиса отправляет в response:
response = {
  type: 0,
  permutation: [2, 0, 1, 4, 3],
  permutedGraphEdges: [
    [0,1], [0,3], [1,2], [1,4],
    [2,0], [2,3], [3,4]
  ]
}
```

**Боб восстанавливает граф:**

```typescript
// Код: Verifier.reconstructGraphFromEdges()
// Боб берёт массив рёбер из response
const edges = [
  [0, 1],
  [0, 3],
  [1, 2],
  [1, 4],
  [2, 0],
  [2, 3],
  [3, 4],
];
const vertexCount = 5; // Боб знает количество вершин из исходного графа G

// Создаёт объект Graph из рёбер
const permutedGraph = new Graph({
  vertexCount: 5,
  edgeCount: 7,
  edges: edges,
});

// Теперь у Боба есть объект Graph, представляющий G'
```

**Что происходит:** Боб получает только список рёбер `[(0,1), (0,3), ...]` и создаёт из них полноценный объект `Graph` с внутренней структурой (список смежности и т.д.).

### Что значит "Сериализует G'ᵢ"?

**Сериализация** — это преобразование объекта Graph в строку для вычисления хеша.

**Пример:**

```
У Боба есть объект Graph с рёбрами:
G' = {
  edges: [[0,1], [0,3], [1,2], [1,4], [2,0], [2,3], [3,4]]
}
```

**Процесс сериализации:**

```typescript
// Код: Verifier.serializeGraph()
// 1. Получает все рёбра из графа
const edges = [
  [0, 1],
  [0, 3],
  [1, 2],
  [1, 4],
  [2, 0],
  [2, 3],
  [3, 4],
];

// 2. Нормализует рёбра (делает u < v для каждого ребра)
// [2,0] → [0,2], [2,3] → [2,3] (уже нормализовано)
const normalized = [
  [0, 1],
  [0, 3],
  [1, 2],
  [1, 4],
  [0, 2],
  [2, 3],
  [3, 4],
];

// 3. Сортирует рёбра (сначала по первой вершине, потом по второй)
const sorted = [
  [0, 1],
  [0, 2],
  [0, 3],
  [1, 2],
  [1, 4],
  [2, 3],
  [3, 4],
];

// 4. Преобразует в строку: "u,v|u,v|..."
const serialized = "0,1|0,2|0,3|1,2|1,4|2,3|3,4";
```

**Зачем нужна нормализация и сортировка?**

- Чтобы сериализация была **детерминированной** (одинаковой каждый раз)
- Алиса и Боб должны получить **одинаковую строку** для одного и того же графа
- Иначе хеши не совпадут!

**Полный процесс проверки коммита:**

```
1. Боб получает response с рёбрами: [[0,1], [0,3], [1,2], ...]

2. Восстанавливает Graph:
   permutedGraph = new Graph({ edges: [[0,1], [0,3], ...] })

3. Сериализует граф в строку:
   serialized = "0,1|0,2|0,3|1,2|1,4|2,3|3,4"

4. Добавляет salt из коммита:
   data = "0,1|0,2|0,3|1,2|1,4|2,3|3,4" + "a3f5b2c8..."

5. Вычисляет хеш:
   computedHash = SHA256(data) = "7f3a8b2c..."

6. Сравнивает с hash из коммита:
   computedHash == commitment.hash?
   Если да → коммит корректен! ✓
```

**Почему это работает:**

- Каждый раунд имеет свою пару `(commitment, response)`
- Коммит связывает конкретный G' конкретного раунда
- Боб проверяет коммит раунда i против G' из response раунда i
- Разные раунды не смешиваются - каждый проверяется независимо

**Пример проверки:**

```
Раунд 1:
  commitment₁ = {hash₁, salt₁} → для G'₁
  response₁ содержит G'₁
  Боб проверяет: SHA256(G'₁ + salt₁) == hash₁? ✓

Раунд 2:
  commitment₂ = {hash₂, salt₂} → для G'₂ (другой граф!)
  response₂ содержит G'₂
  Боб проверяет: SHA256(G'₂ + salt₂) == hash₂? ✓
  (НЕ проверяет G'₂ против hash₁ - это был бы другой раунд!)
```

## Почему это безопасно?

### Zero-Knowledge (нулевое раскрытие)

**Боб не узнает исходный цикл**, потому что:

- В Challenge 0: видит только перестановку и структуру графа, но не цикл
- В Challenge 1: видит цикл в переставленном графе, но не знает перестановку
- Каждый раунд использует **новую** перестановку, поэтому нельзя связать информацию

### Soundness (звучность)

**Алиса не может обмануть**, если не знает цикл:

- Если угадает challenge = 0: может показать любую перестановку
- Если угадает challenge = 1: должна показать цикл, но его нет → провал
- Вероятность угадать все k раундов: 1/2^k (очень мала)

### Completeness (полнота)

**Если Алиса знает цикл**, она всегда может ответить правильно:

- Challenge 0: показывает перестановку и граф
- Challenge 1: показывает переставленный цикл

## Зачем нужен коммит?

**Без коммита:**

```
1. Боб: challenge = 0
2. Алиса: показывает π, G'
3. Боб: challenge = 1 (откатывает назад)
4. Алиса: показывает другой граф G'' с циклом
```

**С коммитом:**

```
1. Алиса: коммит к G' (фиксирует граф)
2. Боб: challenge = 0 или 1 (случайно)
3. Алиса: должна показать тот же G', что закоммитила
4. Боб проверяет коммит → подмена невозможна
```

Коммит **связывает** Алису с конкретным графом G' до выбора challenge.

## Итог

После k успешных раундов:

- **Боб** убежден, что Алиса знает гамильтонов цикл
- **Алиса** не раскрыла, какой именно цикл она знает
- Вероятность обмана: 1/2^k (экспоненциально мала)

**Zero-Knowledge Proof работает!** 🎉
