# Протокол Zero-Knowledge Proof: Алиса и Боб

## Сценарий

**Алиса** знает гамильтонов цикл в графе и хочет доказать это **Бобу**, не раскрывая сам цикл.

**Боб** хочет убедиться, что Алиса действительно знает цикл, но не хочет узнавать, какой именно.

---

## Исходные данные

### Граф G (общий для обоих)

```
5 вершин, 7 рёбер
Рёбра: (0,1), (1,2), (2,3), (3,4), (4,0), (0,2), (1,4)
```

Визуализация:
```
    0 ──── 1
    │ ╲   ╱ │
    │  ╲ ╱  │
    2   ╳   4
    │      │
    └──────┘
        3
```

### Гамильтонов цикл (знает только Алиса)

```
[0, 1, 2, 3, 4]
```

Это цикл: `0 → 1 → 2 → 3 → 4 → 0` (проходит через все 5 вершин ровно один раз)

**Боб НЕ знает этот цикл!** Он знает только граф G.

---

## Протокол: Один раунд

Протокол состоит из трёх фаз: **Commit**, **Challenge**, **Response**.

### Фаза 1: Commit (Коммит)

**Алиса выполняет:**

#### Шаг 1.1: Генерация случайной перестановки

```typescript
// Код: Prover.generateCommitment()
const permutation = generateRandomPermutation(n);
// Например, получается: π = [2, 0, 1, 4, 3]
```

Что это означает:
- Вершина 0 → переименовывается в вершину 2
- Вершина 1 → переименовывается в вершину 0
- Вершина 2 → переименовывается в вершину 1
- Вершина 3 → переименовывается в вершину 4
- Вершина 4 → переименовывается в вершину 3

#### Шаг 1.2: Применение перестановки к графу

```typescript
// Код: applyPermutationToGraph(graph, permutation)
const permutedGraph = applyPermutationToGraph(graph, [2, 0, 1, 4, 3]);
```

**Оригинальный граф G:**
```
Рёбра: (0,1), (1,2), (2,3), (3,4), (4,0), (0,2), (1,4)
```

**Переставленный граф G' = π(G):**
- Ребро (0,1) → (π(0), π(1)) = (2, 0)
- Ребро (1,2) → (π(1), π(2)) = (0, 1)
- Ребро (2,3) → (π(2), π(3)) = (1, 4)
- Ребро (3,4) → (π(3), π(4)) = (4, 3)
- Ребро (4,0) → (π(4), π(0)) = (3, 2)
- Ребро (0,2) → (π(0), π(2)) = (2, 1)
- Ребро (1,4) → (π(1), π(4)) = (0, 3)

**Итого G':**
```
Рёбра: (0,1), (0,3), (1,2), (1,4), (2,0), (2,3), (3,4)
```

**Важно:** Граф G' изоморфен G, но вершины переименованы. Структура та же, но Боб не знает перестановку!

#### Шаг 1.3: Применение перестановки к циклу

```typescript
// Код: applyPermutationToCycle(hamiltonianCycle, permutation)
const permutedCycle = applyPermutationToCycle([0,1,2,3,4], [2,0,1,4,3]);
// Результат: [2, 0, 1, 4, 3]
```

**Оригинальный цикл:** `0 → 1 → 2 → 3 → 4 → 0`  
**Переставленный цикл:** `2 → 0 → 1 → 4 → 3 → 2` (в графе G')

#### Шаг 1.4: Создание криптографического коммита

```typescript
// Код: commitToGraph(permutedGraph, permutation)
const commitment = commitToGraph(permutedGraph, permutation);
```

**Что происходит:**
1. Граф G' сериализуется в строку:
   ```typescript
   // Код: serializeGraph(permutedGraph)
   // Рёбра сортируются: (0,1), (0,3), (1,2), (1,4), (2,0), (2,3), (3,4)
   // Сериализация: "0,1|0,3|1,2|1,4|2,0|2,3|3,4"
   ```

2. Генерируется случайный salt:
   ```typescript
   const salt = crypto.randomBytes(32).toString("hex");
   // Например: "a3f5b2c8d1e9f4a7b6c5d4e3f2a1b0c9d8e7f6a5b4c3d2e1f0a9b8c7d6e5f4"
   ```

3. Вычисляется hash:
   ```typescript
   const hash = crypto.createHash("sha256")
     .update("0,1|0,3|1,2|1,4|2,0|2,3|3,4" + salt)
     .digest("hex");
   // Например: "7f3a8b2c9d1e4f5a6b7c8d9e0f1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9"
   ```

4. Результат:
   ```typescript
   commitment = {
     hash: "7f3a8b2c9d1e4f5a6b7c8d9e0f1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9",
     salt: "a3f5b2c8d1e9f4a7b6c5d4e3f2a1b0c9d8e7f6a5b4c3d2e1f0a9b8c7d6e5f4"
   }
   ```

**Алиса → Боб:** Отправляет только `{hash, salt}`

**Боб получает:** Коммит, но **не может** восстановить граф G' из hash (SHA-256 необратим)!

---

### Фаза 2: Challenge (Вызов)

**Боб выполняет:**

```typescript
// Код: Verifier.generateChallenge()
const challenge = verifier.generateChallenge();
// Случайно выбирается 0 или 1
```

**Боб → Алиса:** Отправляет `challenge = 0` или `challenge = 1`

**Что означает:**
- **Challenge = 0:** "Покажи перестановку π и докажи, что G' = π(G)"
- **Challenge = 1:** "Покажи гамильтонов цикл в G' (без раскрытия перестановки)"

---

### Фаза 3: Response (Ответ)

**Алиса отвечает в зависимости от challenge:**

#### Вариант A: Challenge = 0 ("Покажи перестановку")

```typescript
// Код: Prover.respondToChallenge(0, permutedGraph, permutation)
response = {
  type: 0,
  permutation: [2, 0, 1, 4, 3],  // Раскрывает перестановку π
  permutedGraphEdges: [
    [0,1], [0,3], [1,2], [1,4], 
    [2,0], [2,3], [3,4]
  ]  // Все рёбра графа G'
}
```

**Алиса → Боб:** Отправляет перестановку и все рёбра G'

**Боб проверяет:**

1. **Валидность перестановки:**
   ```typescript
   // Код: Verifier.isValidPermutation(permutation, n)
   // Проверяет: [2,0,1,4,3] - это биекция [0..4] → [0..4]?
   // ✓ Все значения в диапазоне [0..4]
   // ✓ Нет дубликатов
   // ✓ Размер = 5
   ```

2. **Проверка коммита:**
   ```typescript
   // Код: Verifier.verifyRound() → openCommitment()
   // Боб восстанавливает G' из permutedGraphEdges
   // Сериализует: "0,1|0,3|1,2|1,4|2,0|2,3|3,4"
   // Вычисляет: SHA256("0,1|0,3|1,2|1,4|2,0|2,3|3,4" + salt)
   // Сравнивает с commitment.hash
   // ✓ Совпадает!
   ```

3. **Проверка изоморфизма:**
   ```typescript
   // Код: Graph.isIsomorphicTo(permutedGraph, permutation)
   // Проверяет, что G' = π(G)
   // Для каждого ребра (u,v) в G проверяет наличие (π(u), π(v)) в G'
   // ✓ Все рёбра соответствуют!
   ```

**Результат:** Боб убеждается, что G' - это действительно переставленный граф G, но **не узнаёт цикл** (Алиса не показала рёбра цикла).

---

#### Вариант B: Challenge = 1 ("Покажи цикл")

```typescript
// Код: Prover.respondToChallenge(1, permutedGraph, permutation)
// Сначала применяется перестановка к циклу
const permutedCycle = applyPermutationToCycle([0,1,2,3,4], [2,0,1,4,3]);
// Результат: [2, 0, 1, 4, 3]

// Формируются рёбра цикла в G'
const cycleEdges = [
  [2,0], [0,1], [1,4], [4,3], [3,2]
];

response = {
  type: 1,
  permutedGraphEdges: [
    [0,1], [0,3], [1,2], [1,4], 
    [2,0], [2,3], [3,4]
  ],  // Все рёбра графа G'
  cycleEdges: [
    [2,0], [0,1], [1,4], [4,3], [3,2]
  ]  // Рёбра гамильтонова цикла в G'
}
```

**Алиса → Боб:** Отправляет все рёбра G' и рёбра цикла, но **НЕ отправляет перестановку π**!

**Боб проверяет:**

1. **Проверка коммита:**
   ```typescript
   // Код: Verifier.verifyRound() → openCommitment()
   // Боб восстанавливает G' из permutedGraphEdges
   // Проверяет коммит (аналогично Challenge 0)
   // ✓ Коммит корректен!
   ```

2. **Валидность рёбер цикла:**
   ```typescript
   // Проверяет:
   // ✓ cycleEdges.length === 5 (правильное количество)
   // ✓ Все вершины в диапазоне [0..4]
   // ✓ Нет self-loops
   ```

3. **Существование рёбер в графе:**
   ```typescript
   // Код: Verifier.verifyRound()
   for (const [u, v] of cycleEdges) {
     if (!permutedGraph.hasEdge(u, v)) {
       return false;  // Ребро не существует!
     }
   }
   // Проверяет:
   // ✓ (2,0) существует в G'? Да!
   // ✓ (0,1) существует в G'? Да!
   // ✓ (1,4) существует в G'? Да!
   // ✓ (4,3) существует в G'? Да!
   // ✓ (3,2) существует в G'? Да!
   ```

4. **Проверка гамильтонова цикла:**
   ```typescript
   // Код: isValidHamiltonianCycle(cycle, permutedGraph)
   // Восстанавливает цикл из рёбер: [2, 0, 1, 4, 3]
   // Проверяет:
   // ✓ Длина цикла = 5 (все вершины)
   // ✓ Каждая вершина встречается ровно один раз
   // ✓ Все рёбра цикла существуют в G'
   // ✓ Последняя вершина соединена с первой (3 → 2)
   ```

**Результат:** Боб убеждается, что в G' есть гамильтонов цикл, но **не знает**, как он соотносится с исходным графом G (не знает перестановку π).

---

## Множественные раунды

Один раунд даёт вероятность обмана 50% (если Алиса не знает цикл, она может угадать challenge). Поэтому протокол повторяется **k раз** (например, k=5).

**Вероятность обмана без знания цикла:**
- 1 раунд: `1/2 = 50%`
- 5 раундов: `1/2^5 = 1/32 ≈ 3.125%`
- 10 раундов: `1/2^10 = 1/1024 ≈ 0.098%`

### Пример выполнения: k=3 раунда

#### Раунд 1:

```typescript
// Алиса генерирует новую перестановку для каждого раунда!
const permutation1 = [2, 0, 1, 4, 3];
const permutedGraph1 = applyPermutationToGraph(graph, permutation1);
const commitment1 = commitToGraph(permutedGraph1, permutation1);

// Алиса → Боб: {hash1, salt1}
// Боб → Алиса: challenge = 0

// Алиса отвечает:
response1 = {
  type: 0,
  permutation: [2, 0, 1, 4, 3],
  permutedGraphEdges: [[0,1], [0,3], [1,2], [1,4], [2,0], [2,3], [3,4]]
}

// Боб проверяет: ✓ ПРИНЯТО
```

#### Раунд 2:

```typescript
// Алиса генерирует ДРУГУЮ перестановку!
const permutation2 = [1, 3, 0, 2, 4];  // НОВАЯ!
const permutedGraph2 = applyPermutationToGraph(graph, permutation2);
const commitment2 = commitToGraph(permutedGraph2, permutation2);

// Алиса → Боб: {hash2, salt2}
// Боб → Алиса: challenge = 1

// Алиса отвечает:
const permutedCycle2 = applyPermutationToCycle([0,1,2,3,4], [1,3,0,2,4]);
// Результат: [1, 3, 0, 2, 4]

response2 = {
  type: 1,
  permutedGraphEdges: [[0,1], [0,2], [1,3], [1,4], [2,3], [2,4], [3,4]],
  cycleEdges: [[1,3], [3,0], [0,2], [2,4], [4,1]]
  // Перестановка π₂ НЕ раскрыта!
}

// Боб проверяет: ✓ ПРИНЯТО
```

#### Раунд 3:

```typescript
// Алиса генерирует ЕЩЁ ОДНУ перестановку!
const permutation3 = [4, 2, 1, 0, 3];  // ЕЩЁ ОДНА НОВАЯ!
const permutedGraph3 = applyPermutationToGraph(graph, permutation3);
const commitment3 = commitToGraph(permutedGraph3, permutation3);

// Алиса → Боб: {hash3, salt3}
// Боб → Алиса: challenge = 0

// Алиса отвечает:
response3 = {
  type: 0,
  permutation: [4, 2, 1, 0, 3],
  permutedGraphEdges: [[0,1], [0,2], [1,3], [1,4], [2,3], [2,4], [3,4]]
}

// Боб проверяет: ✓ ПРИНЯТО
```

**Итог:** Все 3 раунда прошли → **Доказательство ПРИНЯТО** ✓

Вероятность обмана без знания цикла: `1/2³ = 1/8 = 12.5%`

---

## Почему это безопасно?

### Zero-Knowledge (Нулевое раскрытие)

**Боб не узнаёт исходный цикл**, потому что:

- **Challenge 0:** Боб видит только перестановку π и структуру графа G', но **не видит цикл**
- **Challenge 1:** Боб видит цикл в переставленном графе G', но **не знает перестановку π** (не может связать с исходным графом G)
- Каждый раунд использует **новую** случайную перестановку → нельзя связать информацию между раундами

**Пример:**
- В раунде 1 Боб видит перестановку `[2,0,1,4,3]`, но не видит цикл
- В раунде 2 Боб видит цикл `[1,3,0,2,4]` в G'₂, но не знает перестановку `[1,3,0,2,4]`
- Боб не может связать эти два раунда, чтобы узнать исходный цикл `[0,1,2,3,4]`

### Soundness (Звучность)

**Алиса не может обмануть**, если не знает цикл:

**Сценарий обмана:**
```
Раунд 1:
  Алиса коммитит G'₁ без цикла
  Challenge = 0 → Алиса показывает перестановку ✓ (проходит)

Раунд 2:
  Алиса коммитит G'₂ с циклом (другой граф!)
  Challenge = 1 → Алиса показывает цикл ✓ (проходит)

Раунд 3:
  Алиса коммитит G'₃ без цикла
  Challenge = 1 → Алиса должна показать цикл в G'₃, но его нет → ❌ ПРОВАЛ
```

**Почему обман не работает:**
- Коммит связывает Алису с конкретным графом G' до выбора challenge
- Если Алиса коммитит граф без цикла, а challenge = 1 → провал
- Если Алиса коммитит граф с циклом, а challenge = 0 → должна показать перестановку, но тогда Боб увидит, что G' ≠ π(G) (если Алиса пыталась обмануть)

**Вероятность угадать все k раундов:** `1/2^k` (экспоненциально мала)

### Completeness (Полнота)

**Если Алиса знает цикл**, она всегда может ответить правильно:

- **Challenge 0:** Показывает перестановку π и граф G' = π(G) ✓
- **Challenge 1:** Показывает переставленный цикл π(cycle) в G' ✓

---

## Зачем нужен коммит?

**Без коммита (небезопасно):**
```
1. Боб: challenge = 0
2. Алиса: показывает π, G'
3. Боб: откатывает назад, challenge = 1
4. Алиса: показывает другой граф G'' с циклом (обман!)
```

**С коммитом (безопасно):**
```
1. Алиса: коммитит G' (фиксирует граф через hash)
2. Боб: challenge = 0 или 1 (случайно)
3. Алиса: должна показать тот же G', что закоммитила
4. Боб проверяет коммит → подмена невозможна
```

Коммит **связывает** Алису с конкретным графом G' до выбора challenge. Алиса не может изменить граф после того, как узнала challenge.

---

## Итог

После k успешных раундов:

- ✅ **Боб** убеждён, что Алиса знает гамильтонов цикл
- ✅ **Алиса** не раскрыла, какой именно цикл она знает
- ✅ Вероятность обмана: `1/2^k` (экспоненциально мала)

**Zero-Knowledge Proof работает!** 🎉

---

## Код в действии

Вот как это выглядит в коде:

```typescript
// Инициализация
const prover = new Prover(graph, [0,1,2,3,4]);  // Алиса знает цикл
const verifier = new Verifier(graph);            // Боб знает только граф

// Генерация challenges
const k = 5;
const challenges: (0 | 1)[] = [];
for (let i = 0; i < k; i++) {
  challenges.push(verifier.generateChallenge());
}

// Генерация доказательства
const proof = prover.generateProof(k, challenges);

// Проверка доказательства
const result = verifier.verifyProof(proof, graph);

if (result.valid) {
  console.log("Доказательство ПРИНЯТО");
} else {
  console.log(`Доказательство ОТВЕРГНУТО на раунде ${result.failedRound}`);
}
```

Каждый раунд в `proof.rounds` содержит:
- `commitment`: криптографический коммит к переставленному графу
- `response`: ответ Алисы на challenge (либо перестановка, либо цикл)

Verifier проверяет каждый раунд независимо и принимает доказательство только если все раунды прошли проверку.

